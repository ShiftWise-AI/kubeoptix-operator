1. Install/upgrade the release:
   helm upgrade --install {{ .Release.Name }} . -n {{ include "kubeoptix-analyzer.namespace" . }} --create-namespace

2. Check pods:
   oc get pods -n {{ include "kubeoptix-analyzer.namespace" . }}

3. Check build resources:
   oc get is,bc -n {{ include "kubeoptix-analyzer.namespace" . }}

4. If the Git repository is private, configure source secret:
   Option A: create secret manually and set .Values.build.sourceSecret.name
   Option B: set .Values.build.sourceSecret.create=true with username/token

5. Start image build (first deployment):
   oc start-build {{ include "kubeoptix-analyzer.fullname" . }} -n {{ include "kubeoptix-analyzer.namespace" . }} --follow

6. Check route URL:
   oc get route {{ .Values.route.name }} -n {{ include "kubeoptix-analyzer.namespace" . }}

7. Test API:
   curl -k https://$(oc get route {{ .Values.route.name }} -n {{ include "kubeoptix-analyzer.namespace" . }} -o jsonpath='{.spec.host}')/health
